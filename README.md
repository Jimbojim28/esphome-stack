# ESPHome Discovery

Small Dockerized ESPHome discovery service.

## Run locally

```bash
git clone https://github.com/Jimbojim28/esphome-discovery.git
cd esphome-discovery
docker compose up -d --build
