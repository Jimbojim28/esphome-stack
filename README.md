# ESPHome Discovery

Small Dockerized ESPHome discovery service.

## Run locally

```bash
git clone https://github.com/YOURNAME/esphome-discovery.git
cd esphome-discovery
docker compose up -d --build
